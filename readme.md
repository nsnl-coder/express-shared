# yup schemas for express

### how to install

    $ npm install yups-schema

```javascript
const yup = require('yup');
const {
  reqQuerySchema,
  validateRequest,
  objectIdSchema,
} = require('yups-schema');
```
